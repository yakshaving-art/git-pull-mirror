image: golang:1.10
stages:
  - build
  - test

before_script:
  - go get -u github.com/golang/dep/cmd/dep
  - mkdir -p /go/src/gitlab.com/yakshaving.art
  - cp -r /builds/yakshaving.art/gitlab-pull-mirror /go/src/gitlab.com/yakshaving.art/
  - cd /go/src/gitlab.com/yakshaving.art/gitlab-pull-mirror
  - dep ensure

build:
  stage: build
  script:
    - go build

test:
  stage: test
  script:
    - go test -v -cover ./...
